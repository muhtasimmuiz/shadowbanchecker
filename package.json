{
  "name": "shadowcheck-ai",
  "version": "1.0.0",
  "private": true,
  "description": "X/Twitter shadowban checker demo with an API-ready backend architecture.",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "check": "node --check script.js && node --check server.js && node --check services/xApiService.js"
  },
  "engines": {
    "node": ">=18"
  }
}
